import React, { FC, useEffect, useState, useRef } from "react";

interface Props {
  children: React.ReactNode;
  params: any;
}

export default async function ContactUsLayout({ children, params }: Props) {
  return <>{children}</>;
}
